/**
 * If we need jQuery available in our tests, uncomment this and add jQuery as an npm dependency
 */
// Import jQuery
// const $ = require('./node_modules/jquery/dist/jquery');

// Set it globally so it can be used in any test
// global['$'] = global['jQuery'] = $;